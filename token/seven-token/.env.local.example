# Example environment variables for development
# Copy this file to .env.local and fill in the values

# Solana network settings
SOLANA_NETWORK=localnet
# SOLANA_NETWORK=devnet
# SOLANA_NETWORK=mainnet-beta

# Mint authority keypair (base64 encoded)
# For development only - NEVER use this in production with real tokens
# Export your keypair using: solana-keygen pubkey --outfile keypair.json /path/to/keypair.json
# Then convert to base64: base64 -w 0 keypair.json
MINT_KEYPAIR_BASE64=
